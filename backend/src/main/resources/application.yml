server:
  port: 10029

spring:
  datasource:
    url: jdbc:mysql://49.235.161.106:10011/db4?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: wl
    password: Wl1733811
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      pool-name: LawFirmHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000

  sql:
    init:
      mode: always
      schema-locations: classpath:db/init.sql
      continue-on-error: true

  redis:
    host: 49.235.161.106
    port: 10012
    password: 
    database: 0
    timeout: 10000ms
    lettuce:
      pool:
        min-idle: 0
        max-idle: 8
        max-active: 8
        max-wait: -1ms

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.lawfirm.casemanagement.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: LawFirmCaseManagementSecretKey2024VeryLongSecretKeyForJWT
  expiration: 86400000
  header: Authorization
  prefix: Bearer

logging:
  level:
    com.lawfirm.casemanagement: debug
    org.springframework.security: info

file:
  upload:
    path: /opt/uploads/lawfirm/
